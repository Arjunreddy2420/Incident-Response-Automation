apiVersion: v1
kind: Service
metadata:
  name: incident-api
  labels:
    app: incident-api
spec:
  type: ClusterIP
  selector:
    app: incident-api
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
